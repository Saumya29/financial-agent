name: Keep Render App Alive

on:
  schedule:
    - cron: "*/8 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: |
          echo "Pinging app health endpoint..."
          curl -f https://financial-agent-42bh.onrender.com/api/health || exit 1
